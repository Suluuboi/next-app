import ButtonClient from "./Button"; // Import your reusable button

export default function AddToCart() {
  return (
    <div>
      <ButtonClient className="btn btn-primary">Data</ButtonClient>
    </div>
  );
}
